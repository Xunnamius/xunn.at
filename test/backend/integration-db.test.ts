// ? setupTestDb works if the testing suite succeeds!

describe('::getClient', () => {
  it('works as expected', async () => {
    expect.hasAssertions();
  });
});

describe('::getDb', () => {
  it('works as expected', async () => {
    expect.hasAssertions();
  });
});

describe('::overwriteMemory', () => {
  it('works as expected', async () => {
    expect.hasAssertions();
  });
});

describe('::closeClient', () => {
  it('works as expected', async () => {
    expect.hasAssertions();
  });
});

describe('::destroyDb', () => {
  it('works as expected', async () => {
    expect.hasAssertions();
  });
});

describe('::getNameFromAlias', () => {
  it('works as expected', async () => {
    expect.hasAssertions();
  });
});

describe('::initializeDb', () => {
  it('works as expected', async () => {
    expect.hasAssertions();
  });
});

describe('::hydrateDb', () => {
  it('works as expected', async () => {
    expect.hasAssertions();
  });
});
