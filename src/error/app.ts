// ? Errors specific to this application are defined here.

//import { AppError, makeNamedError } from 'named-app-errors';

//export class MyError extends AppError {}
//makeNamedError(MyError, 'MyError');

export {};
